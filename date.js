const date = new Date();
const datey = date.getFullYear();
const dated = date.getDate();
const datem = date.getMonth();
const dateh = date.getHours();
const datemi = date.getMinutes();
document.write(datey + dated + datem + datemi)
